import { connectToDatabase } from '../../util/mongodb';

export default async (req, res) => {
  // const { db } = await connectToDatabase();
  // let data = req.body;
  // data = JSON.parse(data);
  // let doc = await db.collection('daily').updateOne();
  // res.json({ message: 'ok' });
  // res.json(movies);
  console.log('yolo');
};
